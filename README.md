# Django Проект: Domi Sushi 🍣

## ✅ Завершённые Задания

### ЗАДАНИЕ 1: Веб-страницы и Шаблонизация ✓
- ✅ Создана базовая веб-страница (base.html) с использованием Django шаблонов
- ✅ Создано 3 отдельные страницы:
  - **Домашняя страница** (home.html) - основная страница с информацией о ресторане
  - **Регистрация/Вход** (auth.html) - страница с двумя вкладками для входа и регистрации
  - **Меню** (menu.html) - страница мәзіру с возможностью скачивания PDF
- ✅ Настроены ссылки между страницами используя Django `{% url %}` теги
- ✅ Запущен проектный сервер на http://127.0.0.1:8000/

### ЗАДАНИЕ 2: База Данных и Администрирование ✓
- ✅ Создана модель базы данных `UserProfile` с полями:
  - `user` (OneToOne связь с User)
  - `phone` (телефон пользователя)
  - `address` (адрес пользователя)
  - `date_joined` (дата присоединения)
- ✅ Выполнены миграции: `makemigrations` и `migrate`
- ✅ Создан суперпользователь (администратор)
  - **Логин**: admin
  - **Пароль**: admin123
- ✅ Зарегистрирована модель в админ-панели Django (`admin.site.register`)
- ✅ Подключена БД (SQLite db.sqlite3) к проекту

## 📂 Структура Проекта

```
lab2_2_new/
├── config/                 # Основной конфиг проекта
│   ├── settings.py        # Настройки (INSTALLED_APPS, TEMPLATES, БД)
│   ├── urls.py            # Главные маршруты (включены urls приложения)
│   ├── wsgi.py
│   └── asgi.py
├── users/                 # Django приложение
│   ├── models.py          # Модель UserProfile
│   ├── views.py           # Views для всех страниц
│   ├── urls.py            # URL маршруты приложения
│   ├── admin.py           # Регистрация моделей в админе
│   └── migrations/        # Файлы миграций БД
├── templates/users/       # HTML шаблоны
│   ├── base.html          # Базовый шаблон с навигацией
│   ├── home.html          # Домашняя страница
│   ├── auth.html          # Страница входа/регистрации
│   ├── menu.html          # Страница меню
│   └── profile.html       # Профиль пользователя
├── static/                # Статические файлы
│   ├── style.css          # Стили оформления
│   └── script.js          # JavaScript функции
├── manage.py              # Django управление приложением
└── db.sqlite3            # База данных SQLite
```

## 🔗 Доступные Маршруты

| URL | Описание |
|-----|---------|
| http://127.0.0.1:8000/ | Домашняя страница |
| http://127.0.0.1:8000/menu/ | Страница меню |
| http://127.0.0.1:8000/auth/ | Вход/Регистрация |
| http://127.0.0.1:8000/profile/ | Профиль пользователя (требуется вход) |
| http://127.0.0.1:8000/logout/ | Выход из аккаунта |
| http://127.0.0.1:8000/admin/ | **Админ-панель Django** |

## 🔐 Критерии Аутентификации

### Регистрация
- **Поле**: логин, email, пароль
- **Сохранение**: Пользователь автоматически создаётся в БД с профилем
- **Автовход**: После регистрации пользователь сразу входит в систему

### Вход
- **Поля**: логин и пароль
- **Проверка**: Стандартная Django аутентификация

## 📊 Админ-панель

Для доступа в админ-панель:
1. Перейти на http://127.0.0.1:8000/admin/
2. Ввести:
   - **Логин**: admin
   - **Пароль**: admin123
3. Управление моделями:
   - **Users** - просмотр всех пользователей
   - **User Profiles** - просмотр и редактирование профилей пользователей

## 🎨 Использованные HTML файлы Исходного Проекта

- ✅ **index.html** → **home.html** (адаптирован)
- ✅ **auth.html** → **auth.html** (адаптирован)
- ✅ **menu.html** → **menu.html** (адаптирован)
- ✅ **style.css** → **static/style.css** (скопирован)
- ✅ **script.js** → **static/script.js** (скопирован)

## 🚀 Команды Запуска

```bash
# Перейти в папку проекта
cd c:\Users\user\Desktop\lab2_2_new

# Запустить миграции
python manage.py migrate

# Создать суперпользователя (если не создан)
python manage.py createsuperuser

# Запустить сервер разработки
python manage.py runserver
```

## 🔐 Настройка переменных окружения и секретов

В проекте используется внешний API-ключ (Groq). Никогда не храните секреты в коде — используйте переменные окружения и секреты CI/CD.

- Локальная настройка (PowerShell):
```powershell
# Временная переменная в текущем сеансе
$env:GROQ_API_KEY = "ваш_ключ_здесь"
# Сохранить перманентно в переменных пользователя (Windows)
setx GROQ_API_KEY "ваш_ключ_здесь"
```

- Локальная настройка (Git Bash / WSL / Linux / macOS):
```bash
export GROQ_API_KEY="ваш_ключ_здесь"
```

- Настройка в GitHub (репозиторий):
  - Откройте `Settings` → `Secrets and variables` → `Actions` → `New repository secret`.
  - Добавьте имя `GROQ_API_KEY` и вставьте значение ключа.

- Проверка переменной окружения:
```powershell
# PowerShell
echo $env:GROQ_API_KEY

# Git Bash / Linux
echo $GROQ_API_KEY
```

- Рекомендации:
  - Убедитесь, что `users/views.py` читает ключ через `os.environ.get("GROQ_API_KEY")` (уже настроено).
  - Не коммитьте файлы с секретами; добавьте их в `.gitignore`.
  - Для CI/CD используйте защищённые секреты на платформе (GitHub Actions, Heroku, Azure и т.д.).


## ⚙️ Настройки Django

- **DEBUG**: True (режим разработки)
- **ALLOWED_HOSTS**: [] (localhost)
- **LANGUAGE_CODE**: ru-ru (русский)
- **TIME_ZONE**: Asia/Almaty (Алматы)
- **TEMPLATE_LOADERS**: APP_DIRS: True (автоматическое поиск в app_dirs)

## 📝 Примечания

1. **Статические файлы**: CSS и JS файлы загружаются через `{% static %}` теги
2. **Защита CSRF**: Все формы используют `{% csrf_token %}`
3. **Аутентификация**: Используется встроенная Django система
4. **Сообщения пользователю**: Django messages framework для уведомлений
5. **Шаблоны**: Наследование шаблонов через `{% extends %}`

---

**Дата завершения**: 31.03.2026  
**Статус**: ✅ Полностью готово к работе
